# Benchmark Rewrite Plan (Thesis-Clean)

## NEW COMPACT PLAN

Creative next ideas (high value)
1. Add SchemeId resolver + --scheme now (classic vs future randomized).
2. Add SchemeKeygenParams (seed, randomize flag, maybe security level) so randomized variant is clean.
3. Add attack interface early (AttackOps) even with one brute-force attack first.
4. Export bench as CSV + JSON metadata (seed, scheme, commit hash) for thesis reproducibility.
5. Add a “breakability demo mode” that intentionally weakens parameters and shows attack success rate over n.

